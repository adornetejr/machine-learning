#include "data.h"
#include "info_theory_utils.h"

double
InfoTheoryUtils::CalculateEntropyForValues(std::vector<Value *> values) {
  double sum = 0;
  
  return -sum;
}

std::map<std::string, double>
InfoTheoryUtils::CalculateEntropy(DataSet *data_set) {

  return std::map<std::string, double>();
}
